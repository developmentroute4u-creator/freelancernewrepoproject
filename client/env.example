# Client Environment - copy to .env.local and fill in values
# For Render: set NEXT_PUBLIC_API_URL in Dashboard > Your Service > Environment
# NEXT_PUBLIC_* are baked in at build time.

# API base URL (including /api). For Render: https://your-api.onrender.com/api
NEXT_PUBLIC_API_URL=http://localhost:5000/api
